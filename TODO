#    -*- mode: org -*-

* Fix web hook the update method is not being called correctly

* Add support for running behind a proxy
  see http://flask.pocoo.org/docs/deploying/others/ for proxy setup

* Document code at least a little

* Make Debugging freshbooks requests dependent on debug mode

* Add-on items
** B-Shares
** "Family" memberships
